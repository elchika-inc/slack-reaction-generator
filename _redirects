# Redirects for Slack Emoji Generator

# WWW to non-WWW redirect (if needed)
# https://www.slack-emoji-generator.elchika.app/* https://slack-emoji-generator.elchika.app/:splat 301!

# HTTP to HTTPS redirect (handled by Cloudflare automatically)

# Legacy asset redirects (removed - no longer needed)
# These paths are now handled by the build system

# Backward compatibility redirects
/slack-reaction-generator    /    301
/emoji-generator         /    301
/reaction-generator      /    301

# API endpoints (if future implementation)
# /api/*    /.netlify/functions/:splat    200

# 404 fallback for SPA (disabled to avoid infinite loop)
# /*    /index.html    200